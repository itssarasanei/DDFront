import { Profile } from '@/components/Profile';

import classes from './index.module.scss';

const PatientPage = () => {
  return <Profile className={classes.root} />;
};

export default PatientPage;
